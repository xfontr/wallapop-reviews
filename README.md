# Wallpop reviews

Chrome extension that grabs all reviews for a specific user and injects an AI-made summary based on them.

### Extension folder

Simple TS app that performs a request and embeds the response in the view.

### API folder

Fastify + Redis API that handles the request to the AI model, caches it and ensures rate limiting, etc.
